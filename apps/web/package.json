{
  "name": "@thriftage/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @thriftage/db build && next build",
    "dev": "next dev -p 3001",
    "start": "next start -p 3001",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.mts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@thriftage/db": "workspace:*",
    "@thriftage/shared": "workspace:*",
    "next": "^16.3.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "server-only": "^0.0.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "tailwindcss": "^4.3.3",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
